info:
  name: Start
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:5000/api/entries/start
  body:
    type: json
    data: |-
      {
        "activity_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
