info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:5000/api/auth/login
  body:
    type: json
    data: |-
      {
        "login": "test_user",
        "password": "test_password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
