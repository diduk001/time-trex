# Secrets — copy to .env and fill in real values before running docker-compose
BACKEND_SECRET_KEY=change-me
JWT_SECRET_KEY=change-me-too
FRONTEND_SECRET_KEY=change-me
JWT_ACCESS_TOKEN_EXPIRES=86400
REQUEST_TIMEOUT=10
